export type User = {
  identifier: string;
  username: string;
  groups: string[];
};

export type UserPresentation = {
  identifier: string;
  username: string;
  groups: string[];
};

export type UserTransfer = {
  identifier: string;
  username: string;
  groups: string[];
};
